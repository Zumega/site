import React from 'react';
import './footer.scss';

const Footer = () => {
    return (
        <footer>
            Ross McAdams { new Date().getFullYear() }
        </footer>
    );
};

export default Footer;
